# Sensitive / credentials
auth.json
trust.json
agents/.env.browser
.env
extensions/.env

# Session data (large)
sessions/

# Local data
.data/

# Generated / logs
daily-analysis/
pi-crash.log
retrospective/

# Coverage outputs / reports
coverage/
*coverage*.log
*coverage*-summary*.json
coverage-final.json
lcov.info
*.lcov

# Scripts (local automation)
bin/
local-scripts/

# Daily retro SVG
tmp/daily-retro.svg

# Internal pi data
.pi/
.pi-lens/
state/
.context/
cache/
bookmarks.json

# OS / IDE
.DS_Store

# Dependencies
node_modules/
npm/

# Extension-specific ignores
extensions/.context/
extensions/.gw/
extensions/pi-session-*.html
/cron/

# until presets (personal)
extensions/until-presets/

# usage reporter (personal)
extensions/usage-reporter/

# picky-handoff (symlink to separate repo)
extensions/picky-handoff

# Local/custom skills
# Keep already tracked skills; ignore any new/untracked skill folders by default.
skills/*
skills-archive/*

# claude-mcp-bridge local runtime settings
claude-mcp-bridge-tools.json
claude-mcp-bridge-cache.json
helpers/pi-computer-use/
